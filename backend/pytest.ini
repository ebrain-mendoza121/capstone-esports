[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts = -v --tb=short -p no:cacheprovider
markers =
    unit: Pure function tests with no external dependencies
    integration: Tests that require a running app or database
